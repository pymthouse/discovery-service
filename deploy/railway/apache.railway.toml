# Railway service config for Apache load balancer (public, 2 replicas).

[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/apache/Dockerfile"

[deploy]
numReplicas = 2
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
